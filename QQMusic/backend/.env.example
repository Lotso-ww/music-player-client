# Copy to .env and replace these development-only values before running Compose.
MYSQL_DATABASE=qqmusic
MYSQL_USER=qqmusic_app
MYSQL_PASSWORD=change-me-before-running
MYSQL_ROOT_PASSWORD=change-root-before-running
API_PORT=8080
