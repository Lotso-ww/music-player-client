# P0 现有缺陷修复与工程基线

## 基本信息

- SPEC 编号：P0-01 至 P0-16
- 状态：已完成

## 实际变更

- 播放索引、空列表、零时长和进度比例增加边界保护。
- LRC 解析支持多标签及 1/2/3 位小数，失败时清空旧歌词。
- 元数据等待增加错误和 3 秒超时路径。
- SQLite 使用应用数据目录、显式初始化结果和批量事务。
- 导入和播放列表按本地 URL 去重；推荐分页保留尾项。
- 界面退出按钮统一进入持久化退出流程，并兼容旧工作目录数据库迁移。
- 增加独立 QtTest 工程、构建说明和日志规范。

## 验证记录

- 自动测试：`p0-tests.pro` 的 LRC 和推荐分页回归已在 Qt 5.14.2 + MinGW 7.3 下通过（退出码 0）。
- 手工场景：空播放列表、无歌词切歌、损坏音频、托盘退出、Alt+F4、非项目目录启动。
- 构建结果：Qt 5.14.2 + MinGW 7.3 Debug 应用目标编译通过；P0 QtTest 构建并运行通过（退出码 0）。
- 手工验收：歌词显示与旧格式兼容、收藏/历史/本地歌曲重启恢复、界面按钮隐藏到托盘、托盘退出、Alt+F4 和重复导入均正常。

## 遗留事项

- 元数据解析仍在导入调用上下文中运行，但已改为有界事件驱动；后台扫描归入后续 P1D。
